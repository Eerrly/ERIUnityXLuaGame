public enum EBuffTriggerType
{
    None = 0,
    Default = 1,
    Max,
}

public enum EBuffExitType
{
    None = 0,
    Time = 1,
}

public enum ECollisionDir
{
    Forward = 1,
    Left,
    Right,
    Back
}